 ```let quantasX = 0
  for(let i=0 ; i < arrayDeNumeros.length; i++){
    if(arrayDeNumeros[i] === numeroEscolhido){
      quantasX++  }
  }
      if (quantasX > 0  )
    {return `O número ${numeroEscolhido} aparece ${quantasX}x`
    } else {
      return "Número não encontrado"
    }```